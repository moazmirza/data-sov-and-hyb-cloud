# Shared Infrastructure

Store reusable infrastructure and policy assets here.

Modules should reference shared infrastructure assets rather than duplicating deployment files in each module folder.

## Folders

- `platform/`: Shared platform-level infrastructure assets.
- `arc/`: Reusable Azure Arc machine evidence artifacts (machine, extensions, policy states, and concise summaries).
