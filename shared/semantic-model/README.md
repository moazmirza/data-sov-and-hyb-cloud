# Shared Semantic Model

Store reusable model documentation, measures, and model-specific implementation assets here.

Modules should extend this shared model rather than create disconnected reporting models.
