# Shared Environment Setup

The shared environment should be established once, then reused across modules.

## Baseline Setup Areas

- Shared subscription or landing zone assumptions
- Shared Fabric workspace and workspace permissions
- Shared logging and monitoring baseline
- Shared function hosting assumptions
- Shared connectors and API reachability

Module build guides should reference this baseline instead of repeating global setup steps.
