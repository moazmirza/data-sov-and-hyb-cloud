# Shared Troubleshooting

Common failure areas across modules:

- Missing permissions or connector consent
- Incomplete input data coverage
- Out-of-date reference tables or glossary mappings
- Environment variables not populated from templates
- Module steps executed before foundation setup completed

Start with the shared cause, then move into the selected module's `test.md` for scenario-specific failures.
