# Build

## Implementation Steps

1. Ingest defender-related data through the shared pipeline layer.
2. Add transformation and scoring logic to the shared semantic model.
3. Publish report pages that show posture, gaps, and trend views.
4. Optionally wire conversational read experiences if desired.

## Build Checks

- Defender data joins correctly with shared dimensions
- Coverage metrics are reproducible
- Report pages reflect current source state
